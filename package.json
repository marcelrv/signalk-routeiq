{
  "name": "signalk-routeiq",
  "version": "0.1.0-alpha.1",
  "description": "SignalK Nautical Route Planner - Offline tide and vessel-aware routing",
  "license": "Apache-2.0",
  "keywords": ["signalk-node-server-plugin", "signalk-webapp"],
  "files": [
    "dist",
    "public",
    "plotterext",
    "img",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "signalk": {
    "displayName": "RouteIQ",
    "appIcon": "icon.svg",
    "screenshots": [
      "./img/webapp-route.png",
      "./img/freeboard-plugin.png",
      "./img/freeboard-tides.png",
      "./img/download-manager.png"
    ],
    "recommends": ["signalk-tidal-currents", "signalk-tides", "@signalk/freeboard-sk"]
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && tsc test/routing.test.ts test/navmesh.test.ts test/navmesh-integration.test.ts test/zeelandbrug.test.ts test/bbox-penalty-retry.test.ts test/via-disconnected-leg.test.ts test/dynamic-loading.test.ts test/vessel-validation.test.ts test/plugin-registration.test.ts --outDir dist-test --moduleResolution node --module es2022 --target es2022 --declaration --declarationMap --sourceMap --esModuleInterop --strict --skipLibCheck",
    "build:watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "ts-node --esm src/index.ts",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "test": "node --test --test-force-exit dist-test/routing.test.js dist-test/navmesh.test.js dist-test/navmesh-integration.test.js dist-test/zeelandbrug.test.js dist-test/bbox-penalty-retry.test.js dist-test/via-disconnected-leg.test.js dist-test/dynamic-loading.test.js dist-test/vessel-validation.test.js dist-test/plugin-registration.test.js",
    "test:unit": "node --test --test-force-exit dist-test/unit/*.test.js",
    "test:integration": "node --test --test-force-exit dist-test/integration/*.test.js",
    "deploy": "bash deploy.sh",
    "prepublishOnly": "tsc"
  },
  "dependencies": {
    "@signalk/server-api": "^2.0.0",
    "express": "^4.18.2",
    "signalk-plotterext-bus": "^0.8.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/geojson": "^7946.0.14",
    "@types/node": "^22.0.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "typescript-eslint": "^8.60.1"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "signalK": {
    "pluginConstructor": "pluginConstructor",
    "name": "signalk-routeiq",
    "description": "Offline nautical route planner with tide and vessel-aware A* pathfinding",
    "url": "https://github.com/signalk/signalk-routeiq",
    "scope": "route",
    "configSchema": {
      "type": "object",
      "properties": {
        "routingDataDir": {
          "type": "string",
          "title": "Routing Data Directory",
          "description": "Directory containing .sqlite routing graph files. Leave empty to keep them in the plugin's own data directory, which survives plugin updates.",
          "default": ""
        },
        "safetyMarginDraft": {
          "type": "number",
          "title": "Draft Safety Margin (m)",
          "description": "Under-keel clearance added to design draft",
          "default": 0.3,
          "minimum": 0,
          "multipleOf": 0.1
        },
        "safetyMarginAirDraft": {
          "type": "number",
          "title": "Air Draft Safety Margin (m)",
          "description": "Mast clearance added to design air draft",
          "default": 1.5,
          "minimum": 0,
          "multipleOf": 0.1
        },
        "safetyMarginBeam": {
          "type": "number",
          "title": "Beam Safety Margin (m)",
          "description": "Width clearance added to design beam",
          "default": 2.0,
          "minimum": 0,
          "multipleOf": 0.1
        },
        "defaultCoastDistance": {
          "type": "number",
          "title": "Default Min Coast Distance (NM)",
          "description": "Default minimum distance from coastline in nautical miles",
          "default": 0.5
        },
        "averageSpeedKnots": {
          "type": "number",
          "title": "Average Speed (kn)",
          "description": "Cruising speed used to estimate route duration / ETA",
          "default": 6,
          "minimum": 0.5,
          "multipleOf": 0.1
        },
        "considerTides": {
          "type": "boolean",
          "title": "Consider Tides by Default",
          "description": "Factor tidal currents into route calculation. Needs a tide data plugin: signalk-tidal-currents (real current stations, preferred) and/or signalk-tides (height-derived estimate). With neither installed, routes fall back to plain distance. Clients can override per request.",
          "default": false
        },
        "maxTidalCurrentKnots": {
          "type": "number",
          "title": "Max Tidal Current (kn)",
          "description": "Spring-tide current at full flood/ebb used to scale the estimated tidal flow",
          "default": 2.0,
          "minimum": 0,
          "multipleOf": 0.1
        },
        "tidesApiBase": {
          "type": "string",
          "title": "Tides API Base URL",
          "description": "Server hosting the tide/current data plugins (default: this server)",
          "default": "http://localhost:3000"
        },
        "waypointTolerance": {
          "type": "number",
          "title": "Waypoint Simplification Tolerance (m)",
          "description": "Max deviation from the computed path when reducing it to route waypoints (0 = keep every graph node)",
          "default": 30,
          "minimum": 0
        },
        "wrongWayPenalty": {
          "type": "number",
          "title": "Wrong Way Penalty",
          "description": "Penalty multiplier for traveling against traffic flow",
          "default": 5
        },
        "lineOfSightSampleInterval": {
          "type": "number",
          "title": "Line-of-Sight Sample Interval (m)",
          "description": "Spacing between samples when checking line-of-sight for smoothing",
          "default": 500
        },
        "lineOfSightSearchRadius": {
          "type": "number",
          "title": "Line-of-Sight Search Radius (m)",
          "description": "Radius to search for graph nodes when verifying line-of-sight",
          "default": 0
        },
        "catalogUrl": {
          "type": "string",
          "title": "Database Catalog URL",
          "description": "URL to the routing-index.json catalog for downloading routing databases",
          "default": "https://raw.githubusercontent.com/marcelrv/signalk-router-data/main/routing-index.json"
        },
        "dynamicLoading": {
          "type": "boolean",
          "title": "Dynamic Database Loading",
          "description": "Peek installed databases at startup instead of loading all of them; load each region into memory only when a route actually needs it. Leave off for a single-region deployment — there is nothing to gain and startup behavior is unchanged.",
          "default": true
        },
        "eagerLoadAtPosition": {
          "type": "boolean",
          "title": "Eager-load region at vessel position",
          "description": "With dynamic loading on, load the region under the vessel at startup and as it moves (via navigation.position), so a positioned vessel boots ready-to-route instead of empty. No effect when dynamic loading is off.",
          "default": true
        },
        "loadRadiusNm": {
          "type": "number",
          "title": "Proactive load radius (nm)",
          "description": "Load a region when the vessel is within this many nautical miles of it, before it actually crosses in. 0 = only load the region the vessel is inside.",
          "default": 0
        },
        "maxLoadedRegions": {
          "type": "number",
          "title": "Max loaded regions (dynamic loading)",
          "description": "Only applies with dynamic loading on. Keep at most this many regions loaded in memory, evicting the least-recently-used region once a route finishes and none is in progress. 0 = unlimited (keep every region ever loaded) — uncapped memory growth on long passages.",
          "default": 6
        }
      }
    }
  }
}
